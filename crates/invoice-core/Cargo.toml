[package]
name = "invoice-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = "1.40.0"
chrono = { version = "0.4.38", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
base64 = "0.22.1"
